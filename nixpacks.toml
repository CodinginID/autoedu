# Tentukan direktori aplikasi
nixpacks.app_dir = "app"

[phases.setup]
nixpacks.base_image = "python:3.9-slim"
nixpacks.pkgs = ["python3", "pip"]

[phases.build]
commands = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "gunicorn main:app"
